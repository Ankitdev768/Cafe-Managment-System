Flask==2.2.3
gunicorn==20.1.0  # Optional, if you're deploying with Gunicorn
Jinja2==3.1.2  # Flask's templating engine (comes with Flask, but it's listed here for clarity)
json5==0.9.10  # Optional, if you're using JSON with more flexibility (for example, comments in JSON)
